# The backend tools read `.env` from the REPO ROOT (../.env), the same file the
# Flutter app uses. This example is here only for reference — create the real
# file as `../.env`, not `backend/.env`.
#
# Value comes from the Neon console -> project -> "Connection string".
DATABASE_URL=postgresql://USER:PASSWORD@ep-billowing-star-aozk27aq-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
